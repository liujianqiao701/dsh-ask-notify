# dsh-ask-notify 安装说明

> 给**接收方**看的。**不需要管理员权限**。
>
> 作用：DSH 里的 AI 停下来等你回答时（提问 / 审批 / 计划确认），在页面**右下角**弹提醒卡；
> 你不在这个页面时，再补一条**系统通知** + **标签标题闪烁**。
>
> **两条路，选一条**：第一节是插件市场（推荐，99% 的人走这条，不用敲命令）；
> 第二节起是脚本装法（离线 / 要改源码时用）。

---

## 〇、前提（先确认，缺一条先补）

1. 已经装好 **DSH**，并且**至少跑过一次 `dsh web`** —— 没跑过的话 `%USERPROFILE%\.dsh\profiles\web\` 这个目录还不存在。
2. 用的是 **web 界面**（profile 名固定叫 `web`，所有机器都一样，不用改）。
3. 操作系统 **Windows**。
4. **确认插件是 1.0.4 或更高**（所有 harness 版本都能用）。等待态从哪读，插件自己会判：
   - `0.1.7`+ ：`uiSession.sessionStatus`；
   - `0.1.2 ~ 0.1.6`：`uiSession.pendingInteractions`（**实测 `0.1.5-rc.3` 属这一代**）；
   - `≤ 0.1.1-rc.x`：会话列表行上的 `pendingInteraction`。
   三个都读不到时，插件会**自动探测** `uiSession` / `sessions` 上所有 map 型成员，谁真的装着等待就认谁
   （`sources().discovered` 会列出探测结果）—— 所以 harness 再改名字也不用等作者发新版；
   一个都探测不到才报警（控制台一行 + 右下角一张提示卡），不再"静默不生效"。
   ⚠️ 1.0.1 的 `dsh.client.inject` 里钉着 `@deepseek-ai/dsh-client-runtime`（新代 harness 已无此包）。
   实测它**不会挡住挂载** —— 加载器会静默跳过"图里不存在的依赖"；但它是指向已不发布包的死引用，
   1.0.2 起已清空。1.0.4 起连 cordis 的 `exports.inject`（原为 `["sessions"]`）也清空了：
   **插件不再有任何"必须先存在"的依赖** —— 服务缺失时它照样挂载，照样能用 `selftest()` 证明自己活着。
   装完若"看着已生效却没反应"，第一件事看 `__dshAskNotify.sources()` 的 `used` 字段。
   想确认自己在哪一代：双击 `_verify\diag\check-ask-notify.cmd`，E 段会打印 harness 版本与
   `dsh-client-ui-session present` / `dsh-client-runtime present`。

---

## 一、插件市场安装（推荐，不用敲命令）

DSH 界面里 **设置 → 插件市场 → 搜 `dsh-ask-notify` → 安装**。

装完**看卡片上的状态词**，它直接告诉你还差什么：

| 卡片状态 | 还要做什么 |
| --- | --- |
| **已生效** | **刷新页面**：`F5`，不行再 `Ctrl+F5` |
| **已安装，重启后生效** | **重启 `dsh web`**，然后再 `Ctrl+F5` |
| 已安装，未生效 / 已停用 | 点卡片上的「为什么未生效？」看原因；最常见是「有 agent 正在运行」，停掉 agent 再装一次 |

> ⚠️ **刷新这一步不能省，也不是"多按一次就行"**：插件条目是随**页面启动清单**下发的，
> 页面不重新加载就永远不知道有这个插件 —— 服务端再正常也没用。
> **"装完没效果"绝大多数就是这一条。**

**验证（这一步才算真的装上）**：

1. 浏览器按 `Ctrl+F5`；
2. 按 `F12` → 点 **Console** 标签；
3. **手敲**这一行（不要粘贴），回车：

   ```js
   __dshAskNotify.selftest()
   ```

   右下角弹出演示卡并保持 12 秒 = **成功**；显示 `undefined` = 没加载，看下面的表。

**还是 `undefined`？** 按这个顺序砍：

| 检查 | 含义 |
| --- | --- |
| 命令行/脚本装的，但**没重启过** | 必须重启 `dsh web`（登记在 bundle 层，只在启动时读） |
| 市场卡片写「已生效」但没反应 | 只差刷新页面 |
| 浏览器开 `http://127.0.0.1:3080/` → `Ctrl+U` → `Ctrl+F` 搜 `ask-notify` | 搜得到 = 服务端下发了（问题在页面侧）；搜不到 = 没下发 |
| 控制台 `__dshAskNotify.sources()` | `used` 报的是**真正给出数据的源**（`uiSession.status` / `uiSession.pending` / `list`，也可能是自动探测出来的名字，见 `discovered`）；`"none"` = 一个源都读不到，见〇.4 |
| 市场日志 `%USERPROFILE%\.dsh\profiles\web\.dsh-market\log.ndjson` | `install ... hot=true` = 只差 F5；`hot=false` = 必须重启；**没有 install 行** = 压根没装成 |

> 路由名用包名：`/plugins/dsh-ask-notify/client.js` 对，`/plugins/ask-notify/client.js`（行 id）是 404。
> ⚠️ **但别手敲这个 URL 判活** —— 新代 harness 把路由键改成了带 `?rev=...`，
> **不带 rev 手敲必然 404**（服务端一切正常也一样）。判活一律用上表的 `Ctrl+U`，
> 或控制台 `__dshAskNotify.sources()`。
> 也**别用 `curl`**：有些机器前面有鉴权/门控插件，脚本请求会 401/404，浏览器带 cookie 才是通的。

---

## 二、脚本装法 · 放哪里

**源码目录放哪都行**，建议：

```
D:\dsh-plugins\dsh-ask-notify
```

（把 `dsh-ask-notify` 整个文件夹解压到这里；先确认里面直接能看到 `package.json` 和 `install.ps1`，
不要变成 `D:\dsh-plugins\dsh-ask-notify\dsh-ask-notify\package.json` 这种多套一层。）

然后把整个文件夹**改名为 `dsh-ask-notify` 再放**。改名了也没关系，下面改成对应路径即可。

---

## 三、脚本装法 · 改配置（唯一需要动脑的一步）

打开 `D:\dsh-plugins\dsh-ask-notify\install.ps1`，用记事本改 **1 行**：

| 行号 | 现在是什么（作者机器上的路径） | 改成什么 |
| --- | --- | --- |
| **28** | `$src      = 'D:\playwright-AI\playwright\dsh-ask-notify'` | `$src      = 'D:\dsh-plugins\dsh-ask-notify'` ← **填你实际放的路径，必改** |
| **29** | `$profile  = Join-Path $env:USERPROFILE '.dsh\profiles\web'` | **一般不用改**。只有你把 DSH 的数据目录挪走了（设过 `DSH_HOME` 环境变量）才改，改成 `<你的DSH_HOME>\profiles\web` |

改前：

```powershell
$src      = 'D:\playwright-AI\playwright\dsh-ask-notify'
$profile  = Join-Path $env:USERPROFILE '.dsh\profiles\web'
```

改后：

```powershell
$src      = 'D:\dsh-plugins\dsh-ask-notify'
$profile  = Join-Path $env:USERPROFILE '.dsh\profiles\web'
```

> 路径用 `\` 还是 `/` 都行，脚本自己会转。
> 行号按**行首内容**对，别只看数字 —— 脚本一更新行号就会漂。
> ⚠️ 这两个脚本**必须保持纯 ASCII**（它们没有 BOM，而 Windows PowerShell 5.1 会把无 BOM 文件按 ANSI 解，
> 中文注释会变乱码、甚至破坏解析）—— 所以往里改的时候别加中文。

---

## 四、脚本装法 · 它到底动了哪几处

`install.ps1` 做 4 步（3 处登记 + 1 处清理）—— **知道它们在干什么，出问题好排查**：

| 步 | 位置 | 做什么 |
| --- | --- | --- |
| `[1/4]` | `%USERPROFILE%\.dsh\profiles\web\node_modules\dsh-ask-notify` | 建 **junction（目录联接）** 指向源码目录 —— 相当于"挂进去"，以后改源码不用重装 |
| `[2/4]` | `%USERPROFILE%\.dsh\profiles\web\cordis.patch.yml` | **只删不加**：清掉**历史遗留**的用户层 insert（`- id: ask-notify`）。挂载已改由 bundle 层负责，两处都有会让 `dsh web` 直接起不来并报 `duplicate loader entry id: ask-notify` |
| `[3/4]` | `%USERPROFILE%\.dsh\profiles\web\package.json` 的 `dependencies` | 加一行 `link:` —— 防止以后装别的插件时市场的 `pnpm install` 把 junction 清掉 |
| `[4/4]` | 同一个 `package.json` 的 `dsh.profile.bundles` | **登记激活点** —— bundle 层就是按这份清单组装的，本包自带的 `cordis.patch.yml` 提供 insert |

> **三处登记缺一不可**：只放文件夹不建 junction 不生效；建了 junction 不进 `dsh.profile.bundles` **也不生效**。
> 脚本是**幂等**的（重复跑不会重复追加），每次改文件前会留 `.bak-<时间戳>` 备份。
>
> ⚠️ `[4/4]` 这一步是 2026-09-24 补的：在那之前 `install.ps1` **只登记依赖、不登记 bundles**，
> 而 `dsh web` 启动时**不会**自动补登记（只有 `dsh plugin ...` 会跑那一步回溯）——
> 于是"装好了、重启也没用"。**现在脚本装完仍然必须重启**，原因见下一节。

---

## 五、脚本装法 · 执行安装

打开 PowerShell，粘这几条：

```powershell
cd D:\dsh-plugins\dsh-ask-notify
Unblock-File .\install.ps1
powershell -ExecutionPolicy Bypass -File .\install.ps1
```

* 第 2 条是为了解掉"文件来自网络"的封锁 —— 从压缩包 / 聊天软件传过来的文件会被 Windows 标记，
  不解开可能报"无法加载文件，因为在此系统上禁止运行脚本"。报了这个错就补跑这一条。
* 第 3 条走的是 `-ExecutionPolicy Bypass`，**不需要改系统策略、不需要管理员权限**。

看到这四行就算成功（`link:` 后面是你的路径）：

```
[1/4] junction created
[2/4] no legacy user-layer insert - activation comes from the bundle layer
[3/4] dependency registered: dsh-ask-notify = link:D:/dsh-plugins/dsh-ask-notify (backup kept)
[4/4] registered dsh-ask-notify in dsh.profile.bundles (backup kept)
```

（显示 `already present` / `already registered` / `already contains` 都正常，说明以前装过。）

---

## 六、让它生效 + 验证（脚本 / 命令行装法）

**第 1 步是重启，不是刷新** —— 脚本登记的是 bundle 层，而 **bundle 层只在 `dsh web` 启动时读一次**：

1. 停掉 DSH：在那个窗口里按 `Ctrl+C`；关不掉就 `taskkill /F /IM node.exe /T`
   （注意：这会杀掉本机**所有** node 程序）。
2. 重新启动：`npx @deepseek-ai/dsh web`，等页面起来。
3. 浏览器按 `Ctrl+F5`（**必须**：插件条目随页面启动清单下发，页面不重新加载就不知道有它）。
4. 按 `F12` → Console，**手敲**：

   ```js
   __dshAskNotify.selftest()
   ```

   右下角弹出演示卡 12 秒 → 装好了；显示 `undefined` → 见第八节。
5. **系统通知权限**：第一次点击 / 按键时会自动申请一次，点"允许"即可。
   被拒过就点卡片上的 🔔 重新申请，或者到地址栏左边的站点设置里允许。

> 之后改 `lib\client.js`（客户端半区的代码）**不用重启也不用刷新** —— 服务端按内容哈希经
> `/plugins/events`（SSE）推 `rebuilt`，客户端 HMR 自动热换。
> 但改**挂载条目**（`cordis.patch.yml`）属于 bundle 层，**必须重启**。

---

## 七、卸载

在插件市场里装的：**设置 → 插件管理 → 卸载**（下次启动生效）。

脚本装法 / 想手动清干净：

```powershell
cd D:\dsh-plugins\dsh-ask-notify
powershell -ExecutionPolicy Bypass -File .\uninstall.ps1
```

它撤 4 处：遗留的用户层 insert、junction、`link:` 依赖、**`dsh.profile.bundles` 里的条目**。
源码目录不动（想删自己删）。然后**重启 `dsh web`**。

> ⚠️ **千万别只删文件夹 / 只删依赖而留着 `dsh.profile.bundles` 里的条目**：
> `dsh web` 启动时会解析清单里的**每一个**条目，解析不到就直接拒绝启动，报
> `cannot resolve profile bundle "dsh-ask-notify" ...` —— 那就从"插件没生效"升级成"DSH 起不来"了。
> 真遇到了：把 `package.json` 里 `dsh.profile.bundles` 中的 `"dsh-ask-notify"` 那行删掉即可。

---

## 八、常见问题

| 现象 | 原因 / 处理 |
| --- | --- |
| 装完（市场 / 脚本）**完全没反应** | **九成是没刷新页面**：条目随页面启动清单下发，`Ctrl+F5` 才有。市场卡片写「已生效」也是这个意思 |
| 脚本 / `dsh plugin` 装完没反应 | **必须重启 `dsh web`**（登记在 bundle 层，只在启动时读一次） |
| 刷新后控制台 `__dshAskNotify` 是 `undefined` | 见下面几行；再不行跑 `_verify\diag\check-ask-notify.cmd` 出报告 |
| 控制台 `__dshAskNotify.sources().used` = `"uiSession.status"` / `"uiSession.pending"` / `"list"`（或 `discovered` 里列出的名字） | 取数源正常 → 问题在页面侧（刷新 / 浏览器缓存），或当前**确实没有等待** |
| 控制台 `used: "none"` + 一行 `[dsh-ask-notify] 读不到等待态数据源` + 右下角提示卡 | 已知的源和自动探测都读不到 → 见〇.4；把 `sources()` 输出发给作者 |
| 服务端到底有没有下发 | `Ctrl+U` 搜 `ask-notify`（**别手敲 `/plugins/.../client.js`**：新代该路由带 `?rev=`，手敲必 404） |
| 市场写「重启后生效」 | 市场自己判定这次没法热挂载 —— 重启即可 |
| 跑了脚本报 `plugin source not found` | 第三节 `$src` 那行没改，或路径写错（注意别用中文字符的引号） |
| 报"禁止运行脚本" | 漏了 `Unblock-File .\install.ps1`，或者没用 `-ExecutionPolicy Bypass` |
| 卡片不弹，但 `__dshAskNotify.collect()` 有内容 | 该类等待被你自己关掉了（卡片上的偏好开关，存在 localStorage） |
| 系统通知不弹 | 权限没给；另外**用局域网 IP 打开界面**（如 `http://192.168.x.x:3080`）时浏览器会禁用通知 —— 换成 `http://127.0.0.1:3080` 就有。卡片和标题闪烁不受影响 |
| 装了别的插件后失效 | 插件市场的 `pnpm install` 清掉了 junction —— 重跑 `install.ps1` 即可 |
| 页面完全没反应，`collect()` 返回空数组 | 这**不等于没装上**：本插件只在**真有等待**时才显示东西。先用 `selftest()` 自证 |
| 想直接确认服务端有没有下发 | 浏览器开 `http://127.0.0.1:3080/` → `Ctrl+U` → `Ctrl+F` 搜 `ask-notify`。**别用 `curl`**：前置鉴权插件会让脚本请求 401/404，浏览器带 cookie 才是通的 |

---

## 九、文件清单

```
dsh-ask-notify\
├── 安装说明.md          ← 本文件
├── README.md           原理、自检接口、偏好项、排障（想深究看这个）
├── install.ps1         脚本安装（第三节要改的就是它；纯 ASCII，别加中文）
├── uninstall.ps1       脚本卸载（4 处全撤，含 bundles 条目）
├── package.json        插件声明（dsh.bundle.patch / dsh.client）
├── cordis.patch.yml    **bundle 层挂载条目**（`- insert: - id: ask-notify`）。
│                       这是激活点，不是空文件，别删；也不要往 profile 用户层再抄一份
├── lib\index.js        host 侧空实现（本插件纯浏览器侧）
├── lib\client.js       全部逻辑，唯一真正跑的代码
└── _verify\diag\       只读诊断（check-ask-notify.cmd / 重启并验证.cmd）
```
