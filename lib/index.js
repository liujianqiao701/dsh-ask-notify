// Host-side (cordis) entry: the reminder lives entirely in the browser, so this
// plugin deliberately injects nothing and does nothing on the server.
export const name = 'dsh-ask-notify';
export const inject = [];
export function apply() {}
